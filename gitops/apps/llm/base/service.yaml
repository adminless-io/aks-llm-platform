---
apiVersion: v1
kind: Service
metadata:
  name: llm-serving
  namespace: llm
  labels:
    app: llm-serving
spec:
  type: ClusterIP
  selector:
    app: llm-serving
  ports:
    - name: http
      port: 8000
      targetPort: 8000
